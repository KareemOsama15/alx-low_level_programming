#include <stdio.h>

/**
 * main - program that prints single digit of base 10
 *
 * Return: Always 0(success)
 */
int main(void)
{
	int num;

	int i = 0;

	while (i < 10)
	{
		printf("%d\n", num);
		i++;
	}
	return (0);
}
