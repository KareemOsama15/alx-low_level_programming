#include <stdio.h>

/**
 * main - program
 *
 * Return: Always 0(success)
 */
int main(void)
{
	int i = 0;
	int e = 1;

	while (e > i)
	{
		putchar(i);
		putchar(e);

		i++;
		e++;

		while (ie != 89)
		{
			putchar(',');
			putchar(' ');
		}
	}
	return (0);
}
