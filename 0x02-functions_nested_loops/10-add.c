#include <stdio.h>
#include "main.h"

/**
 * add - function that adds two integers and returns the result
 *
 * @a: parameter contain first number
 * @b: parameter contain second number
 *
 * Return: summation of two numbers
 */
int add(int b, int a)
{
	return (b + a);
	_putchar('\n');
}
